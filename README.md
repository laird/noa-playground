# Noa Playground

Noa playground lets you try the next generation of Noa without any Brilliant hardware. If you have a preview key, enter it into the API key box and try it out.

## Quick Start

This is a static web application that requires no build process. To run locally:

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000 in your browser.

📖 **For detailed build and run instructions**, see [BUILD_AND_RUN.md](BUILD_AND_RUN.md)